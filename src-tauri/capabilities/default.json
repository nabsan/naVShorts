{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow main window to call app commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}